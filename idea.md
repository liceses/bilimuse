# idea：bilibili 音乐下载器

## 简要描述：

b站有很多音乐资源，我常常挂着b站当播放器，但是没有网络或者流量情况下还是想要将音乐保存在本地。以往下载b站音乐的方法是把视频下载下来分离音轨保存为单独的mp3等音频格式。此时还没有歌词，封面，作者，专辑等歌曲信息，还需要用musictag等软件手动打标签，根据音频文件手动校准.lrc歌词。流程繁琐，想要听一首歌需要折腾半天。希望这个项目能够解决痛点做到搜索歌名（或者歌词等支持模糊搜索），选择版本，自动下载，自动打标签，自动寻找适配的歌词文件，自动调整歌词。自动寻找封面集等。

## 目标功能清单：

1. **搜索**：按歌名 / 歌手 / 歌词片段模糊搜索歌曲
2. **选版本**：列出 B 站候选视频版本，选择要下载的版本
3. **自动下载**：从 B 站下载纯音频流（DASH audio-only）
4. **自动打标签**：写入标题 / 歌手 / 专辑 / 封面等 ID3 元数据
5. **自动找歌词**：匹配适配的 .lrc 歌词文件
6. **自动校准歌词**：将歌词时间轴与音频自动对齐（偏移 / 缩放）
7. **自动找封面**：匹配并内嵌封面图

## 调研结论（2026-08）：

### 现有项目对比

| 维度 | go-music-dl | wasm-music 音乐姬 | TuneScout-v2 | MusicFlow |
|---|---|---|---|---|
| 形态/语言 | Go，CLI+Web+TUI+桌面 | 油猴脚本(FFmpeg.wasm) | FastAPI+Vue（闭源镜像） | FastAPI+单页 |
| 成熟度 | 3.7k★，很成熟 | 98★，维护停滞 | 124★，闭源不可审计 | 4 commits，早期 |
| B站搜索 | 走 video 搜索，搜到的是视频非曲目 | 不支持（仅当前视频页） | 宣称支持，风控常失效 | 仅关键词 |
| B站音频 | DASH 纯音频，VIP 可 FLAC | playurl 拿 m4s | 需 Cookie | 默认不启用 |
| 打标签 | 内嵌 ID3/封面/歌词 | 固定 m4a 内嵌 | 不可审计 | mutagen 完整 |
| B站歌词 | **无**（返回空） | AI 字幕兜底 + AI 纠错 | 匹配质量差 | 无 |
| 歌词时间轴自动校准 | **无** | **官方 TODO 未实现** | **无**（issue 证实） | **无** |
| 按歌词全文搜索 | 无 | 无 | 无 | 无 |
| 许可证 | AGPL-3.0 | 无 LICENSE | 无 LICENSE | 无 LICENSE |

### 关键技术可行性

- **B 站无法直接按歌词全文搜索**：官方搜索只索引标题/简介/标签（`hit_columns` 仅此三值），不索引字幕/弹幕。可行链路为**两跳反查**：歌词片段 → Musixmatch(`q_lyrics`) / LRCLIB / 网易云 → 得到歌名+歌手 → B 站 `wbi/search/type` 按标题搜版本。
- **B 站自身不提供歌词**：歌词需来自 LRCLIB（免费无 key，标准 LRC，约 300 万条）、网易云/QQ 逆向接口，或 B 站 AI 字幕（有 `music` 置信度字段可筛演唱段，质量差仅兜底）。
- **时间轴自动校准完全可行**：`lyric-align`（faster-whisper 词级时间戳 + 字符模糊匹配锚定，CJK 优先）、`CTCLRC`（CTC 强制对齐）、`audio-offset-finder`（MFCC 互相关求整体偏移，~0.01s）。
- **B 站 DASH 纯音频流可行**：`fnval=4048`，未登录基础音质，登录 320k / VIP FLAC，需 Wbi 签名 + buvid3 cookie 防 -412 风控。

### 差异化定位

七个目标需求中六个已有成熟覆盖（歌名搜索/选版本/下载/打标签/找歌词/找封面），真正无人做好的只有两点，是本项目的核心价值：

1. **按歌词全文反查歌曲**（歌词库 → 歌名 → B 站选版本）
2. **歌词时间轴自动校准**（whisper 强制对齐 + 指纹偏移校正）

建议**从零构建一个 B 站深度定制的 Python 项目**，不建议 fork go-music-dl（AGPL 传染）或 wasm-music（无 LICENSE + 架构陈旧）。
