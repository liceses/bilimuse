#!/usr/bin/env bash
# MusicalBILI 一键打开配置向导
set -e
cd "$(dirname "${BASH_SOURCE[0]}")"
if [ ! -f .venv/bin/python ]; then
    echo "[MusicalBILI] 未找到虚拟环境，请先运行 ./setup.sh"
    exit 1
fi
exec .venv/bin/python -m musicalbili config "$@"
